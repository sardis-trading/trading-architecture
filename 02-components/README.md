# Level 3 — Components

Placeholder. Zooms into each container from [level 2](../01-containers/) to show its internal components.

Expected contents:
- One subfolder per container (`live-trading/`, `feed-archiver/`, `backtest/`, `param-control/`)
- Component diagrams inside each

TBD.
